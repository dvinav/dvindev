import Nav from './Nav'

const NavBar = () => {
  return (
    <div className="text-sm rtl:text-lg ltr:tracking-wider flex items-center lg:hidden">
      <Nav />
    </div>
  )
}

export default NavBar
